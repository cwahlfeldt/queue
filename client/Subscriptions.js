/*if (Meteor.user()) {
    Meteor.subscribe('queuers', Meteor.userId(), (error) => {
        if (error) {
            return console.log(error.message);
        }

        console.log(Meteor.user().username + ' subscribed to Queuers');
    });
}*/
